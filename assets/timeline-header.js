class TimelineHeader {
    constructor(articleDate, allArticles) {
        this.currentArticle = articleDate;
        this.timeline = allArticles;
        this.render();
    }
    render() {
        // Render timeline header here
    }
} 